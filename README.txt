To deploy application run these instructions in command line:  `npm install && npm start`
After that you can reach application by the address `localhost:8000`

For every reload of page stub-data is generated with random values, this logic is implemented on the server
side. Use url like `host[:port]/data.json` to download JSON file. Or you can directly get it in `/stub-data` folder
in root directory.